# 実装計画 2026-04-21

## 目的

PLAY SCAN の検査能力強化、UI品質改善、タイムアウト時の再スキャン導線整備。

---

## 課題

### 課題1: スキャン中 PLAYWRIGHT が押下できてしまう

DEEP SCAN / MULTI SCAN はスキャン中に disabled + 半透明化されるが、PLAYWRIGHT チェックボックスとラベルは無効化されていなかった。

対応: `lockScanUI()` の disabled / lock リストに `playScanOpt` / `batchPlayOpt` / `playScanLabel` / `batchPlayLabel` を追加。`clearScan()` の unlock 処理にも追加。

### 課題2: 英数字フォントを Roboto Condensed に統一

英数字・ラテン文字が Poppins で表示されていた。

対応: Google Fonts に `Roboto Condensed` を追加。`--font-basic` を `"Roboto Condensed", "Poppins", "Noto Sans JP", sans-serif` に変更。

### 課題3: 「該当なし」バッジ数が 0 になる

SC 単位スコア（`ts.na`）でバッジ表示しているが、BASIC が同一 SC を `pass` で持つ場合に `ts.na=0` になる一方、バケットにはカードが存在するため不一致が発生。

対応: バッジをカード実数（`items.length`）優先に変更。

### 課題4: PLAY SCAN 検査項目が 6 項目で不足

キーボード・フォーカス系のWCAG項目（2.1.2, 2.1.4, 2.4.3, 2.4.11）と DOM静的検査項目（2.4.2, 3.1.1, 1.3.5, 3.3.2, 2.5.3）が未実装。

対応: 9関数を新規追加し、エンドポイントで呼び出す。合計 15 項目。

### 課題5: GAS 報告書ダイアログが PERMISSION_DENIED で開けない

DATASOURCE 型シートを含むスプレッドシートで `getReportTabs()` が失敗。

対応: `SheetType.GRID` 以外のシートを事前スキップ。`getDataRange()` を try/catch で保護。

### 追加対応: EXT SCAN チェックON表示が見えない

EXT SCAN チェックボックスは状態としては `checked` になるが、EXT用の `:checked` CSS がなく、白背景に白いチェックマークとなり選択状態が視認しづらかった。

対応: `#extScanOpt:checked` / `#batchExtOpt:checked` に `--color-ext` 背景を追加。単一・一括のEXT SCANでチェックON表示を確認。

### 追加対応: AAA βを一時停止する

設定UIとAAAレベルボタンをコメントアウトし、保存済み `aaaBeta` や `includeAAA: true` が残っていても DEEP SCAN にAAA項目が混ざらないようにする。

対応: フロントのAAA β UI/参照をコメントアウト、`includeAAA` 送信を `false` 固定、サーバー側も `aaaBeta: false` / `includeAAA: false` 固定。

### 追加対応: タイムアウトしたURLを再スキャンできるようにする

DEEP SCAN でタイムアウトが発生した際、対象URLが失われて再実行しづらい。BASIC / DEEP / MULTI / PLAY / EXT のどのスキャンでも、タイムアウトしたURLを記録し、完了後に再スキャン対象へ戻せるようにする。

対応: `timeoutRetryQueue` を追加し、`AbortError` / `timeout` / `タイムアウト` / `504` を検知。URL・スキャン種別・viewportを記録し、`#timeoutRetryPanel` の「再スキャン対象にセット」で単一/一括入力へ戻す。

---

## 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `public/index.html` | 課題1・3・4、AAA β停止、タイムアウト再スキャンキュー |
| `public/css/style.css` | 課題2・EXTチェックON表示、タイムアウト再スキャンパネル |
| `server.js` | 課題4、AAA β停止 |
| `gas/ReportGenerator.gs` | 課題5 |
| `spec/SPEC_WEB.md` | 仕様更新 |
| `test/TEST_SCAN.md` | テスト更新・追加 |
| `README.md` | 運用上の注意更新 |

---

## 完了フラグ

- [x] 課題1 実装完了: PLAYWRIGHT スキャン中無効化（2026-04-21）
- [x] 課題2 実装完了: Roboto Condensedフォント（2026-04-21）
- [x] 課題3 実装完了: 該当なしバッジ実数化（2026-04-21）
- [x] 課題4 実装完了: PLAY SCAN 15項目化（2026-04-21）
- [x] 課題5 実装完了: GAS PERMISSION_DENIED 修正（2026-04-21）
- [x] 追加対応 実装完了: EXT SCAN チェックON表示修正（2026-04-21）
- [x] 追加対応 実装完了: AAA β停止（2026-04-21）
- [x] 追加対応 実装完了: タイムアウトURL再スキャンキュー（2026-04-21）
